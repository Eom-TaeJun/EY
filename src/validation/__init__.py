"""Independent, fail-closed validation utilities.

The validation package does not build or repair upstream SQL objects.  It
recomputes release evidence from explicit inputs and rejects incomplete
evidence.
"""
