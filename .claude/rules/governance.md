---
paths:
  - "docs/governance/**"
  - "config/**"
  - "PROJECT_STATE.md"
---

# Governance rules

- Preserve historical decisions and status evidence.
- Do not change an approved definition or threshold without a decision entry.
- Only the Orchestrator updates the active Wave and integration status.
- State whether a change is reversible and which downstream outputs it affects.
